# Agentic RAG Application - Assignment 7

This repository contains an advanced Retrieval-Augmented Generation (RAG) agent that utilizes the Model Context Protocol (MCP) to seamlessly fetch, index, and query external knowledge sources. 

The agent uses:
- `fetch_url` to retrieve documents from the web.
- `index_document` to chunk and embed documents into a persistent FAISS vector database.
- `search_knowledge` to retrieve semantically relevant information from the knowledge base to answer complex reasoning tasks.

## Running the Evaluation Suite

To run the automated tests consisting of 8 base queries and 5 custom RAG queries (tested both with and without the FAISS index), simply execute:

```bash
bash run_queries.sh
```

## Sample Final Outputs

Here are the final synthesized outputs generated by the agent for the base evaluation queries (A through H) and custom queries:

### Query A:
**Output:**
 This is a Wikipedia article about Claude Shannon, an American mathematician and electrical engineer who made significant contributions to the field of information theory.

The article provides a detailed biography of Shannon's life, including his early education, career at Bell Labs, and work on the development of the mathematical foundations of communication systems. It also discusses his contributions to cryptography, computer science, and artificial intelligence.

Some key points from the article include:

* Shannon was born in 1916 and grew up in Michigan.
* He earned a degree in electrical engineering from the University of Michigan and later received a Ph.D. in mathematics from Princeton University.
* Shannon worked at Bell Labs for over 30 years, where he made significant contributions to the development of communication systems, including the theory of information transmission.
* He is credited with developing the mathematical foundations of communication systems, including the concept of entropy and the development of the Shannon-Fano coding theorem.
* Shannon was also a pioneer in the field of cryptography, and his work on secure communication systems helped lay the foundation for modern encryption techniques.

The article also mentions some of Shannon's personal life, including his marriage to Betty Moore and his interests in unicycling and juggling.

Overall, this article provides a comprehensive overview of Claude Shannon's life and contributions to the field of information theory.

---

### Query B:
**Output:**
 The article discusses various events, exhibitions, and services available in Tokyo, Japan. Here are some of the key points:

1. **Gaudí: Windows on the Future**: An exhibition at 21_21 Design Sight that explores the role of apertures in Antoni Gaudí's architectural language.
2. **Rental Romance Agencies**: A guide to Japan's rental companion services, where people can rent a friend or partner for a day.
3. **Convenience Store Vitamin and Energy Drinks**: A list of 10 unique energy boosters and vitamin supplements available at Japanese convenience stores.
4. **Things to Do in Tokyo Today**: A section that highlights various events and activities happening in Tokyo on any given day.

Additionally, the article mentions:

* The closure of a popular store, Handsome Cafe, due to financial difficulties.
* The opening of a new Sega-themed cafe in Shibuya.
* Various exhibitions and events happening around Tokyo, such as the Gaudí exhibition and the "Gaudí: Windows on the Future" event.

Overall, the article provides a snapshot of the current cultural and entertainment scene in Tokyo.

---

### Query C (Run 1):
**Output:**
 

---

### Query C (Run 2):
**Output:**
 Mom's birthday is on 15 May 2026. I have also set up reminders for two weeks before that date and on the day itself.

---

### Query D:
**Output:**
 Based on the top resources regarding Python `asyncio` best practices, there is a clear consensus on several fundamental patterns for writing efficient and maintainable asynchronous code. The primary advice shared across these sources includes:

*   **Proper `async/await` Usage:** Always use the `async def` and `await` syntax correctly to define and call asynchronous functions. This ensures that the event loop can properly manage execution and context switching.
*   **Avoid Blocking the Event Loop:** Do not perform long-running, CPU-bound, or synchronous blocking operations (like heavy computations or blocking I/O) directly within an `async` function. Doing so halts the entire event loop, negating the benefits of concurrency.
*   **Effective Use of Tasks:** Utilize `asyncio.create_task()` to schedule coroutines to run concurrently. Managing tasks properly allows you to fire off multiple operations simultaneously rather than awaiting them sequentially, which is essential for performance.

---

### Query E:
> "Index the file papers/attention.md and tell me what the three key contributions of the Transformer architecture are according to this paper."

**Output:**
 

---

### Query F (Run 1):
> "Index every .md file under papers/. Confirm how many chunks were indexed in total."

**Output:**
 A total of 15 chunks have been indexed. This is based on the successful indexing of 5 separate documents (`attention.md`, `cot.md`, `dpo.md`, `lora.md`, and `react.md`), each of which was indexed into 3 chunks.

---

### Query F (Run 2):
> "Across the papers I have indexed, what do they say about chain-of-thought reasoning?"

**Output:**
 Based on the knowledge base, the findings regarding chain-of-thought reasoning are primarily derived from the ReAct paper. The key takeaways are:

*   **Improved Interpretability:** By incorporating reasoning traces, models generate human-like task-solving trajectories that are significantly more interpretable than baseline methods that lack these traces.
*   **Enhanced Performance:** The ReAct approach demonstrates superior performance on interactive decision-making benchmarks, such as ALFWorld and WebShop, outperforming traditional imitation and reinforcement learning methods by absolute success rates of 34% and 10%, respectively.
*   **Efficiency:** These improvements are achieved with minimal prompting, requiring only one or two in-context examples to guide the model's reasoning process.

---

### Query G:
> "Across these papers, how do they handle the credit assignment problem?"

**Output:**
 

---

### Query H:
> "Compare how the ReAct paper and the Chain-of-Thought paper differ in their treatment of intermediate reasoning."

**Output:**
 

---

### Custom Setup:
> "Fetch and index the following pages: https://arxiv.org/html/1706.03762v7, https://arxiv.org/html/2201.11903v6, and the Wikipedia page for 'Prompt engineering'."

**Output:**
 The findings from the fetched Wikipedia page on "Prompt engineering" confirm the following key points:

*   **Definition:** Prompt engineering is the practice of structuring natural language inputs (prompts) to guide generative AI models toward producing more accurate, relevant, or useful outputs. It is closely related to "context engineering," which manages non-prompt inputs like metadata and API tools.
*   **Core Techniques:** Common methods include multi-shot prompting, chain-of-thought prompting, tree-of-thought prompting, and role assignment.
*   **Evolution and Context:** While the role of a dedicated "prompt engineer" gained traction during the 2020s AI boom, the title has seen reduced emphasis as AI models have become more capable of generating their own prompts and general AI literacy has increased among employees.
*   **Advanced Methods:** The field incorporates automated approaches such as retrieval-augmented generation (RAG), graph retrieval-augmented generation (GraphRAG), and automatic prompt optimization to enhance model performance and scope.
*   **Security:** The page also highlights "prompt injection" as a significant cybersecurity concern, where malicious prompts are used to target machine learning models.

---

### Custom Query 1 (With Index):
> "What are the three main differences between self-attention and recurrent layers according to the Attention Is All You Need paper?"

**Output:**
 {"title": "Three main differences between self-attention and recurrent layers", "url": "https://www.geeksforgeeks.org/nlp/attention-vs-self-attention-in-transformers/", "snippet": "The three main differences are: 1. Computational complexity per layer, 2. Minimum sequential operations taking place in each layer, and 3. Maximum path length."}

---

### Custom Query 1 (Without Index):
> "What are the three main differences between self-attention and recurrent layers according to the Attention Is All You Need paper?"

**Output:**
 According to the "Attention Is All You Need" paper, the three main technical differences between self-attention and recurrent layers are:

1.  **Computational Complexity per Layer:** Self-attention layers have a computational complexity of $O(n^2 \cdot d)$ per layer, where $n$ is the sequence length and $d$ is the representation dimension. In contrast, recurrent layers have a complexity of $O(n \cdot d^2)$.
2.  **Parallelization:** Self-attention layers allow for total parallelization of computations across all positions in a sequence, whereas recurrent layers require sequential computation, which limits parallelization.
3.  **Path Length for Long-Range Dependencies:** Self-attention layers provide a constant path length of $O(1)$ for learning dependencies between any two positions in a sequence. Recurrent layers require a path length of $O(n)$ to connect two positions, making it more difficult to learn long-range dependencies.

---

### Custom Query 2 (With Index):
> "Explain how the architecture detailed in the 2017 Google brain paper handles input tokens without using sequential processing."

**Output:**
 The Transformer architecture, introduced in the 2017 Google Brain paper "Attention Is All You Need," replaces the sequential processing found in Recurrent Neural Networks (RNNs) with a parallelized mechanism. Instead of processing tokens one by one in a fixed order, the architecture handles the entire input sequence simultaneously through the following key components:

*   **Self-Attention Mechanism:** This is the core innovation that allows the model to weigh the importance of different tokens in a sequence relative to one another, regardless of their distance. By calculating attention scores for every token against every other token in the input, the model creates a global representation of the sequence in a single pass.
*   **Positional Encoding:** Because the architecture processes all tokens at once and lacks an inherent sense of order (unlike RNNs, which process tokens sequentially), it injects "positional encodings" into the input embeddings. These encodings provide the model with information about the relative or absolute position of tokens within the sequence, allowing it to maintain the necessary context of word order.
*   **Parallelization:** By eliminating the recurrent dependency—where the hidden state of one step depends on the previous step—the Transformer allows for massive parallelization during training. This enables the model to process entire sequences of data at once, significantly increasing computational efficiency and allowing for the training of much larger models on larger datasets.

In summary, the Transformer architecture achieves non-sequential processing by treating the input as a set of tokens that interact globally via self-attention, while using positional encodings to preserve the structural information that would otherwise be lost without sequential iteration.

---

### Custom Query 2 (Without Index):
> "Explain how the architecture detailed in the 2017 Google brain paper handles input tokens without using sequential processing."

**Output:**
 The architecture introduced in the 2017 "Attention Is All You Need" paper (the Transformer) replaces sequential processing—characteristic of Recurrent Neural Networks (RNNs)—with a self-attention mechanism. This allows the model to process all input tokens in parallel rather than one by one.

The key mechanisms enabling this non-sequential approach include:

*   **Self-Attention Layers:** Unlike recurrent layers that must wait for the hidden state of the previous time step to process the current token, self-attention layers compute the representation of each token by attending to all other tokens in the sequence simultaneously. This creates direct paths between all positions in the input, regardless of their distance.
*   **Positional Encodings:** Because the architecture processes the entire sequence at once and lacks an inherent sense of order, it injects "positional encodings" into the input embeddings. These encodings provide the model with information about the relative or absolute position of tokens in the sequence, compensating for the loss of sequential structure.
*   **Parallelization:** By eliminating the dependency on previous time steps, the model can perform matrix multiplications across the entire sequence length at once. This significantly improves computational efficiency during training, as it allows for massive parallelization on hardware like GPUs, whereas RNNs are inherently constrained by their sequential nature.

---

### Custom Query 3 (With Index):
> "According to the Chain-of-Thought Prompting paper, exactly how does this method improve performance on arithmetic reasoning tasks?"

**Output:**
 Chain-of-Thought (CoT) prompting improves performance in arithmetic reasoning tasks by decomposing complex, multi-step problems into a series of manageable intermediate reasoning steps. Instead of forcing the model to map an input directly to an output, CoT encourages the model to generate a "chain of thought"—a sequence of logical steps that mirror the human problem-solving process.

This mechanism provides several key advantages:
*   **Decomposition:** It breaks down a difficult arithmetic problem into smaller, sequential sub-problems, which reduces the cognitive load on the model for each individual step.
*   **Interpretability and Debugging:** By explicitly generating intermediate steps, the model's reasoning process becomes transparent, allowing users to identify exactly where a potential error in logic or calculation occurred.
*   **Allocation of Computation:** It allows the model to dedicate more computational resources (in the form of generated tokens) to the reasoning process before arriving at the final answer, effectively increasing the "thinking time" spent on the problem.

In essence, CoT shifts the model's behavior from a single-step prediction to a structured, iterative process, which significantly increases accuracy on tasks that require logical planning and sequential calculation.

---

### Custom Query 3 (Without Index):
> "According to the Chain-of-Thought Prompting paper, exactly how does this method improve performance on arithmetic reasoning tasks?"

**Output:**
 Chain-of-Thought (CoT) prompting improves arithmetic reasoning performance by encouraging large language models to generate intermediate reasoning steps before arriving at a final answer. Instead of attempting to map a question directly to an output, the model decomposes complex, multi-step problems into a series of logical, sequential sub-tasks.

This approach enhances performance through several key mechanisms:

*   **Decomposition of Complexity:** By breaking down a multi-step arithmetic problem into smaller, manageable parts, the model reduces the cognitive load required for each individual step, making it less likely to make errors in calculation or logic.
*   **Increased Transparency and Debugging:** The generation of intermediate steps provides a "trace" of the model's reasoning process. This allows for better interpretability, as the steps can be inspected to identify exactly where a potential error occurred.
*   **Improved Allocation of Computation:** CoT effectively allows the model to allocate more "compute" to the reasoning process by generating more tokens. This extra space for computation enables the model to maintain state and perform sequential operations that are difficult to execute in a single-step generation.

In essence, CoT transforms a challenging "black-box" inference task into a structured, step-by-step derivation, which significantly increases the accuracy of models on tasks requiring logical planning and arithmetic precision.

---

### Custom Query 4 (With Index):
> "Based on the indexed documents, which technique allows models to break down complex logic into step-by-step derivations before arriving at the final answer?"

**Output:**
 The technique that allows models to break down complex logic into step-by-step derivations is known as **Chain-of-Thought (CoT) prompting** (or reasoning).

Instead of forcing a model to jump directly from a prompt to a final answer, this approach encourages the model to generate a series of intermediate reasoning steps. By articulating these logical steps, the model can decompose multi-step problems into more manageable components, which significantly improves performance on tasks requiring arithmetic, commonsense reasoning, and symbolic manipulation. This process effectively creates a "chain" of thought that makes the model's internal logic more transparent and accurate before it arrives at the final conclusion.

---

### Custom Query 4 (Without Index):
> "Based on the indexed documents, which technique allows models to break down complex logic into step-by-step derivations before arriving at the final answer?"

**Output:**
 Chain-of-Thought (CoT) prompting. This technique encourages models to generate intermediate reasoning steps, which significantly improves performance on complex tasks such as arithmetic, commonsense reasoning, and symbolic manipulation. By articulating the logical progression toward a solution, the model is better able to manage multi-step problems that might otherwise lead to errors if attempted in a single step.

---

### Custom Query 5 (With Index):
> "Compare how the ReAct methodology and standard Prompt Engineering instruct the model differently when handling complex tasks."

**Output:**
 ReAct (Reason + Act) and standard prompt engineering represent fundamentally different approaches to how Large Language Models (LLMs) process tasks:

*   **Standard Prompt Engineering:** Typically relies on the model's internal knowledge to generate a direct response to a prompt. While techniques like "Chain-of-Thought" encourage the model to articulate its reasoning steps, the process remains entirely internal and static. The model does not interact with the outside world or verify its information during the generation process.
*   **ReAct:** Integrates reasoning with external action. Instead of relying solely on internal weights, a ReAct-enabled model generates "thought" traces alongside "action" steps (such as querying a Wikipedia API or searching a database). This allows the model to retrieve up-to-date information, correct its own misconceptions, and ground its reasoning in external facts.

**Key Differences:**
1.  **Information Source:** Standard prompting is limited to the model's training data, whereas ReAct can dynamically fetch external information to solve tasks.
2.  **Interactivity:** ReAct creates a feedback loop where the model's actions influence its subsequent reasoning, making it more robust for complex, multi-step tasks.
3.  **Interpretability:** Because ReAct explicitly separates "thought" from "action," the resulting task-solving trajectories are often more transparent and easier for humans to audit compared to the "black box" output of standard prompting.

---

### Custom Query 5 (Without Index):
> "Compare how the ReAct methodology and standard Prompt Engineering instruct the model differently when handling complex tasks."

**Output:**
 The primary difference between ReAct (Reason + Act) methodology and standard Prompt Engineering lies in how they manage the model's cognitive process and interaction with external environments.

*   **Standard Prompt Engineering** typically relies on static instructions or few-shot examples to guide the model toward a desired output. It often focuses on the "what" (the final answer) or uses techniques like Chain-of-Thought (CoT) to encourage internal reasoning, but the model remains largely isolated from external tools or real-time feedback during the generation process.
*   **ReAct Methodology** explicitly instructs the model to interleave reasoning traces with action steps. It forces the model to break down complex tasks into a sequence of "Thought," "Action," and "Observation" cycles. By generating a thought, performing an action (such as a search or API call), and then incorporating the observation from that action into its next thought, the model can dynamically adjust its strategy, correct errors, and gather necessary information that was not present in its initial training data.

In summary, while standard prompt engineering focuses on refining the model's internal logic or output format, ReAct transforms the model into an agent that actively probes its environment to solve complex, multi-step problems.

---

